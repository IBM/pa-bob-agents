# Facilitator Setup — Environment Values

Fill in the values below before each session. Share the completed table (or a printed copy) with participants at the start of the workshop — keep this page open during the session.

**Workshop lab repo:** https://github.com/IBM/pa-bob-agents

---

## Section 1 — IBM Planning Analytics (TechZone)

TechZone shared instance — used in Lab 0, Lab 1, Lab 2.

**Planning Analytics URL:**

| Placeholder | Your Value |
|-------------|------------|
| `<TECHZONE_HOST>` | |
| `<PORT>` | |
| `<YOUR_TENANT_ID>` | |
| `<PA_USERNAME>` | `pm` or `administrator` |
| `<PA_PASSWORD>` | |

---

## Section 2 — SalesLens (CRM + ERP)

Mock API — used in Lab 2, shared for all participants.

**Demo UI:** 

| Placeholder | Your Value |
|-------------|------------|
| `<SALESLENS_ENDPOINT_URL>` | |
| `<SALESLENS_API_KEY>` | `workshop-demo-key` |

---

## Section 3 — watsonx Orchestrate Tenant

| Placeholder | Your Value |
|-------------|------------|
| `<WXO_HOST_URL>` | |
| `<YOUR_ORCHESTRATION_ID>` | |
| `<YOUR_CRN>` | |
| `<YOUR_AGENT_ID>` | |

